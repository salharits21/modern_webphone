These files should not be used directly and might be loaded in very old browsers only (will not work in modern browsers). 
It might be used internally by the softphone skin very rarely, only for users using some very old browser (such as IE 6), redirecting the old browsers (with no modern HTML5/JS support but with java applet support) to this skin. 
